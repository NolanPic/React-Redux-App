const initialState = {
    events: [],
    startDate: '',
    endDate: '',
    error: null
};

export default (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
};
